name: Get Citation Data

on:
  page_build:
  schedule:
    - cron: '0 8 * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Explain skip
        run: |
          echo "This repository no longer includes ./google_scholar_crawler, so the legacy citation crawler workflow is disabled."
          echo "If you want scholar stats later, add the crawler back or replace this workflow with a maintained implementation."
