name: Get Citation Data

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow disabled
        run: |
          echo "Legacy Google Scholar crawler disabled because ./google_scholar_crawler is not present in this repository."
          echo "Enable a new maintained workflow if scholar stats are needed in the future."
